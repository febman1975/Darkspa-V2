{
  "name": "darkspa-antibot",
  "version": "0.1.0",
  "private": true,
  "description": "DarkSpaAntibot web app (React + Express + MongoDB) with anti-bot risk scoring",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm install --prefix client && npm run build --prefix client",
    "start": "npm run start --prefix server",
    "start:safe": "npm run start:safe --prefix server",
    "postinstall": "npm ci --prefix server && npm ci --prefix client",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
